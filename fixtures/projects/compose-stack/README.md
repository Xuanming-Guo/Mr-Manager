# Compose stack fixture

Synthetic Docker Compose project used by Desktop Manager tests. It intentionally contains
diagnostic issues so Compose Doctor can produce deterministic results without touching real
user projects or Docker state.
